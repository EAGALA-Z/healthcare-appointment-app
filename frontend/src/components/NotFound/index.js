import {NotFoundContainer, NotFoundHeading, NotFoundText} from './styledComponents'

const NotFound = () => (
  <NotFoundContainer>
    <NotFoundHeading>404 - Page Not Found</NotFoundHeading>
    <NotFoundText>Sorry, the page you are looking for does not exist.</NotFoundText>
  </NotFoundContainer>
)

export default NotFound
